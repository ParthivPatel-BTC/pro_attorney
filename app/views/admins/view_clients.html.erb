<%= render 'users' %>

<% data= UserProfile.pluck(:first_name)%>

<%data1=User.pluck(:last_sign_in_at).map{|d| d.strftime(" %S %d-%m-%Y %H")}%>
<%dt=data.zip(data1)%>

<%= column_chart dt ,discrete:true%>
